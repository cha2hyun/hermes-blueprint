# === Hermes blueprint 공통 시크릿 — install.sh가 각 프로파일 .env로 전파 ===
# 이 파일을 `.env`로 복사한 뒤 값을 채우고 `./install.sh`를 실행하세요.
# (.env 는 git에서 제외됩니다. 이 .env.example 만 추적됩니다.)

# Anthropic Console 종량 API 키 (모든 프로파일 공용 — 구독 OAuth 아님)
ANTHROPIC_API_KEY=
